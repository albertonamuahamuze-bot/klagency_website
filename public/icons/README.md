# KLAGENCY pillar icons

Replace these files to change the three pillar icons without editing React code:

- `pillar-innovation.svg`
- `pillar-sustainable.svg`
- `pillar-results.svg`

Keep the same filenames, or update the `iconImage` paths in `lib/site-content.ts`.
